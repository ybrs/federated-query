
The decorrelation north star + remaining-gap table have been merged into
`tasks.md`:

  Fallback (the plan to close every gap).
  north star & remaining gaps" (the gap table with its fail-fast tests).

This stub can be deleted.
